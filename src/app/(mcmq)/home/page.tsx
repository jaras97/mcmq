import HeroCarousel from '@/components/hero-carousel';
import React from 'react';

const page = () => {
  return (
    <div className='text-primary'>
      <HeroCarousel />
    </div>
  );
};

export default page;
